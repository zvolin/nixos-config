diff -ruN a/skills/brainstorming/SKILL.md b/skills/brainstorming/SKILL.md
--- a/skills/brainstorming/SKILL.md	2026-04-11 20:44:16.246998081 +0200
+++ b/skills/brainstorming/SKILL.md	2026-04-11 21:00:21.426399647 +0200
@@ -22,46 +22,66 @@
 You MUST create a task for each of these items and complete them in order:

 1. **Explore project context** — check files, docs, recent commits
-2. **Offer visual companion** (if topic will involve visual questions) — this is its own message, not combined with a clarifying question. See the Visual Companion section below.
-3. **Ask clarifying questions** — one at a time, understand purpose/constraints/success criteria
-4. **Propose 2-3 approaches** — with trade-offs and your recommendation
-5. **Present design** — in sections scaled to their complexity, get user approval after each section
-6. **Write design doc** — save to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` and commit
-7. **Spec self-review** — quick inline check for placeholders, contradictions, ambiguity, scope (see below)
-8. **User reviews written spec** — ask user to review the spec file before proceeding
-9. **Transition to implementation** — invoke writing-plans skill to create implementation plan
+2. **Check ferrex memory** — run `/recall` and `/reflect` for prior decisions, context, contradictions
+3. **Offer visual companion** (if topic will involve visual questions) — this is its own message, not combined with a clarifying question. See the Visual Companion section below.
+4. **Ask clarifying questions** — one at a time, understand purpose/constraints/success criteria
+5. **Propose 2-3 approaches** — explicit pros/cons per approach; auto-select if one clearly dominates
+6. **Present design** — include pros/cons for choices between alternatives, get user approval after each section
+7. **Write design doc** — save to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`; commit unless gitignored
+8. **Update ferrex memory** — run `/remember` for key decisions, `/forget` for superseded memories
+9. **Spec review loop** — dispatch `spec-document-reviewer` subagent from `spec-document-reviewer-prompt.md`; loop until 0 critical/medium issues; max 10 iterations then escalate to user
+10. **User reviews written spec** — ask user to review the spec file before proceeding
+11. **Select execution strategy (heuristic, no ask)** — apply the Strategy Selection Heuristics (see section below) to the task list implied by the design. Pick ONE strategy. Do NOT ask the user to confirm. Announce:
+
+    > "Selected execution strategy: **<strategy>**. Reason: <one line grounded in the heuristics>."
+
+    Strategies (only two — there is no sequential mode):
+    - **Parallel subagents** → writing-plans → subagent-driven-development. Default.
+    - **Agent teams** → writing-plans → Agent teams. For large features with cross-cutting coordination.
+12. **Transition to implementation** — invoke writing-plans. Writing-plans copies `## Execution Strategy` from the spec into the plan header verbatim. Then execute with the strategy recorded in the plan — no further confirmation.

 ## Process Flow

 ```dot
 digraph brainstorming {
     "Explore project context" [shape=box];
+    "Check ferrex memory" [shape=box];
     "Visual questions ahead?" [shape=diamond];
     "Offer Visual Companion\n(own message, no other content)" [shape=box];
     "Ask clarifying questions" [shape=box];
     "Propose 2-3 approaches" [shape=box];
-    "Present design sections" [shape=box];
+    "Present design" [shape=box];
     "User approves design?" [shape=diamond];
     "Write design doc" [shape=box];
-    "Spec self-review\n(fix inline)" [shape=box];
+    "Update ferrex memory" [shape=box];
+    "Spec review loop" [shape=box];
     "User reviews spec?" [shape=diamond];
-    "Invoke writing-plans skill" [shape=doublecircle];
+    "Select strategy (heuristic)" [shape=box];
+    "Invoke writing-plans skill" [shape=box];
+    "Parallel subagents\n(subagent-driven-dev)" [shape=doublecircle];
+    "Agent teams" [shape=doublecircle];

-    "Explore project context" -> "Visual questions ahead?";
+    "Explore project context" -> "Check ferrex memory";
+    "Check ferrex memory" -> "Visual questions ahead?";
     "Visual questions ahead?" -> "Offer Visual Companion\n(own message, no other content)" [label="yes"];
     "Visual questions ahead?" -> "Ask clarifying questions" [label="no"];
     "Offer Visual Companion\n(own message, no other content)" -> "Ask clarifying questions";
     "Ask clarifying questions" -> "Propose 2-3 approaches";
-    "Propose 2-3 approaches" -> "Present design sections";
-    "Present design sections" -> "User approves design?";
-    "User approves design?" -> "Present design sections" [label="no, revise"];
+    "Propose 2-3 approaches" -> "Present design";
+    "Present design" -> "User approves design?";
+    "User approves design?" -> "Present design" [label="no, revise"];
     "User approves design?" -> "Write design doc" [label="yes"];
-    "Write design doc" -> "Spec self-review\n(fix inline)";
-    "Spec self-review\n(fix inline)" -> "User reviews spec?";
+    "Write design doc" -> "Update ferrex memory";
+    "Update ferrex memory" -> "Spec review loop";
+    "Spec review loop" -> "Spec review loop" [label="issues found"];
+    "Spec review loop" -> "User reviews spec?" [label="clean"];
     "User reviews spec?" -> "Write design doc" [label="changes requested"];
-    "User reviews spec?" -> "Invoke writing-plans skill" [label="approved"];
+    "User reviews spec?" -> "Select strategy (heuristic)" [label="approved"];
+    "Select strategy (heuristic)" -> "Invoke writing-plans skill";
+    "Invoke writing-plans skill" -> "Parallel subagents\n(subagent-driven-dev)" [label="strategy=parallel"];
+    "Invoke writing-plans skill" -> "Agent teams" [label="strategy=teams"];
 }
 ```

-**The terminal state is invoking writing-plans.** Do NOT invoke frontend-design, mcp-builder, or any other implementation skill. The ONLY skill you invoke after brainstorming is writing-plans.
+**Terminal state:** writing-plans → execute with the strategy recorded in the plan's header (Parallel subagents or Agent teams). There is no sequential mode. Do NOT invoke any other skill — writing-plans is the only next step after brainstorming. Do NOT ask the user to pick a strategy — the heuristic selection is now fixed in the spec and plan.

@@ -77,11 +97,30 @@
 - Only one question per message - if a topic needs more exploration, break it into multiple questions
 - Focus on understanding: purpose, constraints, success criteria

+**Checking ferrex memory:**
+
+- After exploring project context, silently run `/recall` to search for prior decisions, designs, or context related to the current topic
+- Run `/reflect` to check for stale or contradictory memories
+- If contradictions are found, use `/forget` to remove the incorrect entries
+- Surface any relevant findings to inform the brainstorming — mention them when presenting approaches if they affect the design
+
 **Exploring approaches:**

 - Propose 2-3 different approaches with trade-offs
+- Every approach MUST list explicit **Pros** and **Cons**
 - Present options conversationally with your recommendation and reasoning
 - Lead with your recommended option and explain why
+- If one option dominates on all axes, auto-select it and explain why (user can override)
+
+**Pros/Cons in design documents:**
+- Architecture decisions get a "Pros / Cons / Why we chose this" block
+- Format: simple lists, no elaborate tables
+- Proportional to decision weight — a minor choice gets one line each, a major choice gets a paragraph
+
+Example:
+> **Pros:** Reproducible, version-pinned, clean patch mechanism
+> **Cons:** Must maintain patch when upstream updates
+> **Why we chose this:** The alternative (post-activation scripts) is fragile and not reproducible

 **Presenting the design:**

@@ -111,29 +150,83 @@
 - Write the validated design (spec) to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
   - (User preferences for spec location override this default)
 - Use elements-of-style:writing-clearly-and-concisely skill if available
-- Commit the design document to git
-
-**Spec Self-Review:**
-After writing the spec document, look at it with fresh eyes:
+- If the path is not gitignored (check with `git check-ignore -q <path>`), commit the design document to git. If it is gitignored, skip the commit — the file is saved locally and usable. Never use `git add -f`.

-1. **Placeholder scan:** Any "TBD", "TODO", incomplete sections, or vague requirements? Fix them.
-2. **Internal consistency:** Do any sections contradict each other? Does the architecture match the feature descriptions?
-3. **Scope check:** Is this focused enough for a single implementation plan, or does it need decomposition?
-4. **Ambiguity check:** Could any requirement be interpreted two different ways? If so, pick one and make it explicit.
+**Updating memory:**

-Fix any issues inline. No need to re-review — just fix and move on.
+- After the user approves the design, run `/remember` to store key design decisions and rationale
+- If any existing memories are superseded by the new design, use `/forget` to remove them
+- Keeps ferrex in sync with the latest decisions
+
+**Spec review loop:**
+After writing the spec document:
+1. Dispatch `spec-document-reviewer` subagent using the template at `skills/brainstorming/spec-document-reviewer-prompt.md`
+2. Provide the subagent with the spec file path and precise review context — never session history
+3. If issues found, fix them and re-dispatch
+4. Loop until 0 critical/medium issues remain
+5. Max 10 iterations, then escalate to user

 **User Review Gate:**
 After the spec review loop passes, ask the user to review the written spec before proceeding:

-> "Spec written and committed to `<path>`. Please review it and let me know if you want to make any changes before we start writing out the implementation plan."
+> "Spec written to `<path>`. Please review it and let me know if you want to make any changes before we start writing out the implementation plan."

 Wait for the user's response. If they request changes, make them and re-run the spec review loop. Only proceed once the user approves.

-**Implementation:**
+**Select execution strategy:**
+Apply the Strategy Selection Heuristics (section below) to auto-select Parallel subagents or Agent teams. Announce the choice with a one-line reason. Do not ask the user.
+
+**Transition to implementation:**
+- Invoke the writing-plans skill to create a detailed implementation plan
+- Then execute with the selected strategy
+- Do NOT invoke any other skill. writing-plans is the next step after brainstorming.
+
+## Strategy Selection Heuristics
+
+Apply these rules in order. Take the first match. Record the match and reason in the spec's `## Execution Strategy` section. There are only two strategies — no sequential mode.
+
+1. **Agent teams** when ALL of:
+   - ≥4 independent tasks.
+   - Cross-cutting coordination is required (shared integration points, negotiated contracts, coordinated schema/API changes across streams).
+   - Reviews need to look at work holistically across streams, not just per-task.
+   - The feature is large enough that a persistent team with messaging pays off.
+
+2. **Parallel subagents** — the default in every other case. Use this when:
+   - 1–3 tasks, OR
+   - ≥4 tasks that can be worked on independently with clear file ownership and no inter-task communication, OR
+   - Any work where per-task review is sufficient.
+
+   A single-task job still uses parallel subagents — you just dispatch one subagent and run the two-stage review.
+
+**Tiebreakers:**
+- If torn between parallel and teams, choose parallel — it is simpler and has lower coordination overhead.
+- Multi-language designs are NOT a reason to escalate to teams — parallel subagents with different agent types handles it.
+
+**Do NOT ask the user to confirm the selection.** Announce it and write it into the spec. The user can countermand it explicitly; otherwise proceed.
+
+## Humanize All Non-Code Prose
+
+Apply the `humanizer` skill to all non-code text throughout brainstorming. This includes:
+
+- **Design presentations** (approach proposals, design walkthroughs)
+- **Spec documents** (the written design doc saved to disk)
+- **Plan prose** (goal descriptions, architecture summaries, task descriptions)
+- **Questions and explanations** (clarifying questions, approach reasoning)
+
+**Does NOT apply to:** code blocks, file paths, commands, configuration snippets, structured data (tables, checklists).
+
+**How to apply:** Strip AI-isms inline as you write — don't produce slop then clean it up. Watch for:
+- Significance inflation ("pivotal", "crucial", "vital role", "testament")
+- Promotional language ("vibrant", "groundbreaking", "seamless")
+- Copula avoidance ("serves as", "stands as" instead of "is")
+- Superficial -ing phrases ("highlighting", "underscoring", "fostering")
+- Rule-of-three lists, negative parallelisms ("not just X, but Y")
+- Filler ("in order to", "it is important to note")
+- Sycophantic openers ("Great question!", "Absolutely!")
+- Generic positive conclusions ("the future looks bright")
+- Em dash overuse, excessive boldface, emoji decoration

-- Invoke the writing-plans skill to create a detailed implementation plan
-- Do NOT invoke any other skill. writing-plans is the next step.
+Write like a person talking to another person. Vary sentence rhythm. Have opinions. Be specific.

 ## Key Principles

diff -ruN a/skills/brainstorming/spec-document-reviewer-prompt.md b/skills/brainstorming/spec-document-reviewer-prompt.md
--- a/skills/brainstorming/spec-document-reviewer-prompt.md	2026-04-11 20:44:16.246998081 +0200
+++ b/skills/brainstorming/spec-document-reviewer-prompt.md	2026-04-11 20:44:16.250998082 +0200
@@ -16,13 +16,33 @@

     ## What to Check

+    ### Critical Issues (must fix before proceeding)
+
     | Category | What to Look For |
     |----------|------------------|
     | Completeness | TODOs, placeholders, "TBD", incomplete sections |
     | Consistency | Internal contradictions, conflicting requirements |
     | Clarity | Requirements ambiguous enough to cause someone to build the wrong thing |
-    | Scope | Focused enough for a single plan — not covering multiple independent subsystems |
-    | YAGNI | Unrequested features, over-engineering |
+    | Dependencies | External dependencies or prerequisites not mentioned |
+    | Testability | Success criteria that cannot be verified |
+
+    ### Medium Issues (should fix)
+
+    | Category | What to Look For |
+    |----------|------------------|
+    | Pros/Cons | Architecture decisions without "Pros / Cons / Why we chose this" blocks |
+    | Alternatives | Major decisions without considering alternatives |
+    | Testing | No concrete verification steps |
+    | File list | No clear list of files to be created or modified |
+    | Scope | Boundaries that could cause confusion during implementation |
+
+    ### Minor Issues (nice to fix)
+
+    | Category | What to Look For |
+    |----------|------------------|
+    | Verbosity | Sections that could be more concise |
+    | References | References to concepts not defined in the document |
+    | Ordering | Sections that would read better in a different order |

     ## Calibration

@@ -37,13 +57,26 @@

     ## Spec Review

-    **Status:** Approved | Issues Found
+    **Critical issues**: <count>
+    **Medium issues**: <count>
+    **Minor issues**: <count>
+
+    ### Critical Issues
+    - [C1] <description> — suggested fix: <fix>
+
+    ### Medium Issues
+    - [M1] <description> — suggested fix: <fix>
+
+    ### Minor Issues
+    - [m1] <description> — suggested fix: <fix>
+
+    If 0 critical and 0 medium issues:

-    **Issues (if any):**
-    - [Section X]: [specific issue] - [why it matters for planning]
+    **Status:** Approved
+    **No critical or medium issues found.** Spec is ready for user review.

     **Recommendations (advisory, do not block approval):**
     - [suggestions for improvement]
 ```

-**Reviewer returns:** Status, Issues (if any), Recommendations
+**Reviewer returns:** Status with severity-categorized issues, or Approved with optional recommendations.
