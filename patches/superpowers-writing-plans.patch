diff -ruN a/skills/writing-plans/SKILL.md b/skills/writing-plans/SKILL.md
--- a/skills/writing-plans/SKILL.md	2026-04-10 02:24:13.342049802 +0200
+++ b/skills/writing-plans/SKILL.md	2026-04-10 02:24:13.342093135 +0200
@@ -7,7 +7,7 @@

 ## Overview

-Write comprehensive implementation plans assuming the engineer has zero context for our codebase and questionable taste. Document everything they need to know: which files to touch for each task, code, testing, docs they might need to check, how to test it. Give them the whole plan as bite-sized tasks. DRY. YAGNI. TDD. Frequent commits.
+Write comprehensive implementation plans assuming the engineer has zero context for our codebase and questionable taste. Document everything they need to know: which files to touch for each task, code, testing, docs they might need to check, how to test it. Give them the whole plan as bite-sized tasks. DRY. YAGNI. TDD. KISS.

 Assume they are a skilled developer, but know almost nothing about our toolset or problem domain. Assume they don't know good test design very well.

@@ -40,7 +40,6 @@
 - "Run it to make sure it fails" - step
 - "Implement the minimal code to make the test pass" - step
 - "Run the tests and make sure they pass" - step
-- "Commit" - step

 ## Plan Document Header

@@ -49,7 +48,7 @@
 ```markdown
 # [Feature Name] Implementation Plan

-> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
+> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or Agent teams to implement this plan task-by-task, per the Execution Strategy below. Steps use checkbox (`- [ ]`) syntax for tracking.

 **Goal:** [One sentence describing what this builds]

@@ -57,6 +56,10 @@

 **Tech Stack:** [Key technologies/libraries]

+## Execution Strategy
+
+[Copy verbatim from the spec's `## Execution Strategy` section. Must name exactly one of: **Parallel subagents**, **Agent teams**. Include the one-paragraph reason. Do NOT add a Sequential option — it does not exist. This is authoritative — subagent-driven-development reads from here. Do NOT ask the user to change it.]
+
 ---
 ```

@@ -95,12 +98,6 @@
 Run: `pytest tests/path/test.py::test_name -v`
 Expected: PASS

-- [ ] **Step 5: Commit**
-
-```bash
-git add tests/path/test.py src/path/file.py
-git commit -m "feat: add specific feature"
-```
 ````

 ## No Placeholders
@@ -113,11 +110,32 @@
 - Steps that describe what to do without showing how (code blocks required for code steps)
 - References to types, functions, or methods not defined in any task

+## Document Pros and Cons
+
+When a plan picks between alternatives (library, architecture pattern, data structure, approach), include a brief pros/cons/why block:
+
+> **Pros:** [advantages]
+> **Cons:** [disadvantages]
+> **Why we chose this:** [reasoning]
+
+Proportional to decision weight — a minor choice gets one line each, a major choice gets a paragraph. If one option clearly dominates, auto-select and state why.
+
+## Prose Quality
+
+All non-code text in the plan (descriptions, context, rationale) must read like a person wrote it. Apply the `humanizer` skill patterns — strip AI-isms as you write, don't produce slop then clean it up.
+
+**Applies to:** Goal statement, Architecture summary, task descriptions, explanatory notes.
+**Does NOT apply to:** code blocks, file paths, commands, expected output, step labels.
+
+Watch for: significance inflation, promotional language, copula avoidance ("serves as" instead of "is"), superficial -ing phrases, filler, generic conclusions, rule-of-three padding. Be direct and specific.
+
 ## Remember
 - Exact file paths always
-- Complete code in every step — if a step changes code, show the code
+- Complete code in the plan — every function, type, and constant must appear in full somewhere. Later steps may reference earlier-defined code by name instead of repeating it.
 - Exact commands with expected output
-- DRY, YAGNI, TDD, frequent commits
+- DRY, YAGNI, TDD, KISS
+- Reuse existing utilities and dependencies before introducing new ones
+- Follow the existing conventions and style used in the project, unless there's a clear reason not to

 ## Self-Review

@@ -133,20 +151,16 @@

 ## Execution Handoff

-After saving the plan, offer execution choice:
-
-**"Plan complete and saved to `docs/superpowers/plans/<filename>.md`. Two execution options:**
-
-**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration
+The plan's `## Execution Strategy` section is authoritative. Do NOT re-ask the user. After saving the plan, announce:

-**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints
+> "Plan complete and saved to `docs/superpowers/plans/<filename>.md`. Strategy from spec: **<strategy>**. Proceeding with <sub-skill>."

-**Which approach?"**
+Then transition directly based on the strategy:

-**If Subagent-Driven chosen:**
+**Parallel subagents:**
 - **REQUIRED SUB-SKILL:** Use superpowers:subagent-driven-development
 - Fresh subagent per task + two-stage review

-**If Inline Execution chosen:**
-- **REQUIRED SUB-SKILL:** Use superpowers:executing-plans
-- Batch execution with checkpoints for review
+**Agent teams:**
+- Spawn an Agent team with the appropriate teammates
+- The team-lead enforces two-stage review gates: spec compliance review first, then code quality review after each task
